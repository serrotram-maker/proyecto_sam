# project-sam
This project analyzes SAM files using a Python script with the [Rich] library to produce formatted terminal output. The workflow is automated with [Nextflow] and dependencies are managed with [uv].
